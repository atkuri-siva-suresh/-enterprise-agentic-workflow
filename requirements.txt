# Core LangGraph + LangChain (let pip resolve compatible versions)
langgraph>=0.2.66
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-community>=0.3.0

# Anthropic SDK (direct usage for tool use patterns)
anthropic>=0.40.0

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0

# UI
streamlit>=1.41.0

# Utilities
python-dotenv>=1.0.0
rich>=13.9.0
httpx>=0.28.0
tenacity>=9.0.0

# Dev / Testing
pytest>=8.3.0
pytest-asyncio>=0.25.0
